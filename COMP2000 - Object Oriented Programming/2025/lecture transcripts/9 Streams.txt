SPEAKER 0
So, just to remind ourselves, what is a stream? It's something that works a little bit like a collection, so you guys are familiar with the the idea of a collection by now. So things like uh a a list, so array lists, and I know you guys have probably focused mostly on those, on all the units you've you've touched on them with, but there's also notions of other collections like sets. So other words, groups of data that are made up of other kinds of data types. A stream is a similar idea, but the difference, essentially is, there's a few of them we'll outline here, but the main difference to consider is that if I think about an array list, We asked at the very beginning of the semester, what's the difference between that and just a good old-fashioned array like you learned about in first year. Do you guys remember what the main difference was? What was the main difference between an array and an array list? The size, so what's the, what is the primary difference? That's right, so when you create an array, it's fixed size. You go and say please create me an array and it's n elements in size when you create it. An array list can grow and shrink. So that's much more flexibility. However, at any particular point in time, an array list has a fixed size. You can make it bigger, you can make it smaller, but it, you stop and pause a program at any particular time and say, how long is my array? It'll be however long it is. A stream does not have a size. A stream is notionally just an endless thing that you can consume. So it's a bit like, I know this is a bit old school for you guys, but do you guys remember DVDs, those things your parents use, they have a movie on them and that's it. It's like the difference between that and YouTube, and YouTube not just something that's pre-recorded, but live streams on YouTube, which can go on forever. So it's in the name, streams have that kind of notion that they can go on, Potentially forever, um, they may terminate at some point, but they're not, in a, in the same defined way that an an array or an array list would be. Now the other thing that's really important about the way streams work is that they're kind of much more lively. So what do I mean by that? Well, the stream could be a video stream, but what other kind of data can you think of that might be a stream of data that you might want to consume in an application? What other kinds of data might you want to consume? Music, yep, so some live sounds. What else? Think more commercially. Let's imagine you you leave this place and you end up at a bank. Stock market, right, so there's a stream of data, so it's things that you consume from the real world, they're dynamic, they change, they don't necessarily have an end. So The thing that's generating that data, whether it's a YouTube feed or the stock market or whatever, it's a set of dynamic processes. Those dynamic processes could be on the computer, or they could be things that are being pulled from the real world. So there's this notion of things happening in parallel, and we'll come back to that idea later. Um, The other thing that you'll notice, and hopefully you've done this in other units already. How many of you are looking at that going, ooh. Select from Does that look familiar to everybody? Select name from. Is anybody not familiar with it? Yeah, SQL, so databases, right? So you guys would have done in your database, uh, units things that look a little bit like this, and I think you probably would have done a lecture that some of you might have gone ugh, but relational algebra, do you remember doing that? Yes, no, maybe Blank stares Yes? No, some of you didn't do it, OK, that's OK. Um, so the the the important element to consider about this is that when you're doing a database queer, what you're really doing is some maths. You're drawing a relationship between sets of data, and it's a little bit like what you would've done in high school about unions and intersections and those kinds of things. So there's a mathematical underpinning to this. And those kind of mathematical underpinnings are really about looking at a set of data and saying, I wanna find things that match, and then if they match, I wanna do something with them. I either want to include them or exclude them, so possibly filtering out data. And if you think about a stream of data, it might be very much that, you think about your stock market ticker. You've got stock coming in from all the different companies around the world. Some of it might be, you know, Apple, Nvidia, all those kind of tech companies, but it could be mining companies, it could be all sorts of things. Maybe you're writing a program that only processes mining company data. So you're gonna get this raw stream of data that's got every every stock in it. And the only thing you're really interested in is the mining data. So you wanna be able to filter that stream and exclude certain values. So these are the kinds of operations that you might want to ah employ as you're working on that data. And then streams allow you to do these kinds of expressions, so even though they don't look like an SQL query, there is some commonality in what they do. OK, so, um, the rest of this book, or at least the rest of the these couple of chapters, talk about this, um, they've got a, uh, an example that they worked through, which is, uh, dishes of food and having a stream of those, and we're gonna process those. So some of the things might be the amount of calories a dish has in it. Um So let's have a quick look at this bit of code here. So most of these streaming features were introduced in Java 8. There's been some incremental improvement in the, in the version since, but prior to Java 8, you would have written a program perhaps like this. We wanna go and create a, A collection that contains all the dishes that we're interested in, but what we're specifically interested in is low calorie dishes. So we're gonna go and create a, an array of that, or an array list, excuse me. And then we're gonna do An iteration, so we're gonna just do the simple loop, we're gonna go through and grab all the dishes in our menu. And if the dish has less than 400 calories, we're gonna say that that's a low calorie dish. So, we're gonna generate this list that's only got, Dishes that match a certain criteria. Now The next thing we might want to do is sort them, so maybe we're interested in the dishes that are low calorie, but we wanna actually know, well, hang on, what order can we order those in uh in an order based on the number of calories in that dish? And then the last thing to do is to then perhaps uh add that to our low caloric dishes, so in other words, we're gonna create a separate list completely that's that same set of data but sorted in the way that we want. So does that block of code kind of make sense to you, given that that's the criteria of what we're gonna do. We wanna go through an existing list of, of, of data, we want to extract only a subset of that data, and then we wanna sort that that subset into an ordering that we'd like. So again, very much like you might want to do with SQL but the the structures are very different. If you were to be given that task, could you see that that would be probably the most likely way you'd implement the code? Using that same basic structure. I mean you might use different variable names, but ultimately you'd probably do that same thing, create myself a list, go through and add things to that list based on a a criteria. And then sort that into a new array to give me the sorted form. So this is the way that you would normally write your code. So, we're filtering. We then want to sort the dishes, and we want to process the sorted list. Now, one of the things, as I alluded to earlier, is that writing code like this kind of means that we end up with an extra bunch of stuff flying around that we don't really need in some sense. These are temporary or garbage variables. And you know, every time we write code, most of us, our programs are kind of littered with this. We've got a little method, we go and get some input data, we go and create a local variable. We'll do some stuff on it, we might create yet another local variable, do some more stuff on it, and then those, those variables just disappear. And so they're placeholders, and there's a kind of a meme with this, but it's a very true one. I'll just see how many of you struggle with this. When you're writing code, how much time do you spend thinking about well what am I gonna call my variable? Be honest. Yeah, I spend a ton of time, cause I like my code to be readable, generally, and it's like, oh, what am I, but I've already used that name and I've kind of come up with some other stupid name for it, and it's, it's a little bit troubling. And you can see it here, so low calorie dishes, low caloric dishes, low caloric dishes name. You can see that whoever authored this is already struggling to come up with some sensible names. In some sense though, wouldn't it be nice if we could ditch the idea of having the names entirely? So using strings, we can actually achieve that end. So, pulling in streams, we're gonna start off with our menu, so that's the starting point we had before. And rather than Going through and creating these superfluous and temporary variables of lists, we're gonna just simply apply, A bunch of what we call predicates or or or methods in what we refer to as a pipeline. Now, you guys probably are not used to this syntax, but you know you know that you can actually take a really wide line of Java, and unless you split a keyword or a method, you can actually just take any of that and stack the code vertically and it still will execute. So this is logically speaking, one line of code, we have our beginning of our declaration here, and then we have our closing semicolon here. So even though I've spread this over 123456 lines on the screen, it's really one line logically of code. So it's a one line implementation, and it's doing exactly the same thing. We're taking our menu, which is an array list. And it turns out array lists have been augmented from Java 8 with a method that says turn this thing into a stream. So take, take an array or an array list, produce a stream from it. And once we've got a stream, we can start applying these methods to it. So the first one is a filtering method, and the filtering method takes, what's this thing here, what what is this thing that's in between the round brackets? Can you tell me what that is? It's the thing we looked at in week 8. It's a lambda, right, so we're sticking a function in there that says for each element of the list. Or the stream, in actual fact. It's, it's the variable inside a lambda. Right? So We're saying for every element of the list. Check the calories and check that it's below 400. Now what the filter method does is it says if that lambda, which is a boolean, so it's a predicate. Matches, it gets kept in the list, if it's not, it gets thrown away. It then passes that onto the sorted method. And uses a comparison method that we pass in, so we tell it how we're doing our sorting. Now, one of the things that's kind of a bit difficult to deal with when you're doing this in Java is, as I've said, we've augmented Java with this. We've added filters which are the, sorry, streams, which is this kind of functional style of programming. But we're building it on top of an already existing traditional object-oriented model. And the majority of our code base is actually probably gonna look like the object-oriented, not the the functional style. So we need tools to transfer between these two different idioms. So ideally you'd like to be able to just deal with all your data as a stream all the time. But to exist with the, or to coexist with the existing Java code, you wanna be able to convert between. So we saw that the array methods or the array list has this stream method that says, hey, take my array, turn it into a stream. But in all likelihood, once I've applied my pipeline of operations, I probably want to turn it back into an array list or a list of some kind. And that's what's called a collector. So at the very end, we're gonna collect the results of the stream and turn it back into a list. OK, now as I as I sort of alluded to there, ideally you could just avoid these two steps completely and keep everything as a stream throughout your whole program. And it would be possible to do that. If you're starting your program from scratch. But most of the time in in the real world, you don't get that luxury. You don't rewrite everything from scratch, you have to integrate with an existing source base. So you see this a lot in, A lot of post Java 8 code that you'd be using streams, but you're gonna keep switching between having them in regular collections, array lists, sets, whatever that happens to be, and then moving them to uh streams. OK. Now, the great benefit of using streams is that you can actually change things up and work on a multi-core processor and actually have the job farmed out to multiple processors. And the Java runtime will do that automatically, and the only thing you really need to do is change that call from stream to parallel stream. And if it's possible to farm the data out into multiple cores to be processed simultaneously, Java will do that for you, and it will take care of the details. Now it turns out that's actually quite clever cos actually getting that right is very, very difficult. A lot of, a lot of work has been put into that, uh, and it works quite well. We'll come back to that, I think probably next week we'll talk more about the parallel aspect. Don't stop comparing this, is it the variables.

SPEAKER 1
this Sorry, this Oh We covered this a couple of

SPEAKER 0
weeks ago, so this is actually passing in the method. By reference. It's a method No, I mean the the niche. That's a class. That's the class, yep. OK. OK, now things to note about this, when we say things are written in a functional style, what we're really talking about is it's a declarative style. You'll notice here that we took away all the four loops. The 4 loops are telling us how to do things, so what do I mean by that? Let's go back to that original version, up here. And we'll notice that this thing is very step by step, it's like a recipe for making a cake. Do this thing 10 number of times, and when that's done, do this next thing 10 number of times, and so on and so forth. So it's a very step by step process. So it's very low level, and it's the way of, As a human interacting with the computer on the computer's terms. Computers are very mechanical things, they do things in a very low level way. Do this thing 5 times, do this other thing 7 times, and then move on. And so that's fine to make the computer do something, but is that necessarily the way you actually talk to other humans? I mean it might be. Especially if you've been programming long enough. But how do you normally tell people, if I, if you told me you wanted me to, if I asked you guys how do I get to the library from here? How would you tell me to do it? I'm assuming you all know where the library is. What would you say to me? Yeah, see? Walk out the door, turn right, and then you'll see the library. Is that very mechanical? I mean if I was to do that mechanically, if I was telling the computer to do it. What would you have to say to the computer? I mean, you know, let's count it, 1234, I won't do the rest, I think you get the idea. I'd have to tell it how many steps to move. I then I'd have to tell it how to open the door, right? It's all this very low level, do this, do this, do this, do this. But when we actually communicate with things, we'd rather just give relatively high level instructions. We want to declare what to do, not tell it mechanically what to do. So it's the what to do, not how to do it. If you look at this, it's more in that style, it's saying, hey, just filter the streams that match this criteria. There's no loops here Right, can you see that that's a much more abstract way of actually telling the computer what to do? Yeah. So what do you think the benefit of doing things in that way is? Right, so I'm gonna say that this is a much more abstract, high level or declarative way of telling the computer what to do. Is there a, is there a benefit to that? What do you think? Too early after 2 weeks away. Is that the answer? Maybe Alright, let me start the ball rolling. Because I've said to do it in a more abstract way, I've been able to leave out some of the detail, some of the do things 10 number of times. How many times have you had a program that you've written and you've had an error that you've spent 5 seconds, 5 hours, somewhere in between debugging, because you got the number of steps wrong? You know, the old off by 1 error. Do this 10 times and you should've done it 9 times or 11 times, right? So, is that even a possible problem here? There is no do it end time, so I can't make that error. What other things are there about this that might be advantageous? So we just saw here that I can change that keyword to parallel, and that says, hey, if I've got multiple CPUs, just figure out how to do it across those CPUs. How would I do that? In this world, What do you guys think? That to split through, right? I'd have to split the array up, so I'd have to know how big it is, so already I'm delving into that, I could get the numbers wrong territory. Let's say it had 1000 elements, and I had 4 CPUs, I'd have to split it up into 4 blocks of 250. Right? So I'd have to restructure my code already. And then what happens if I pick up my code and move it to a computer with 8 CPUs? I have to change it again, right? So it means that I'm very tied to either executing things on one core, wasting all that extra resource I've got, or having to rewrite my code every time I moved it from one computer to another. Right, that's one of the problems of doing things on a multi-core computer. It's difficult to deal with that portability issue. The code becomes very unportable. This, I'm just telling the computer, you go figure it out, not my problem, and it will do it. So that higher level way of defining what I wanna do is very advantageous for a number of reasons. It turns out also, it makes it easier if you're actually into this kind of thing of actually coming up with a proof that the program works the way it's supposed to. Yep. Sorry, sir, so you're saying uh you'd use parallel string,

SPEAKER 1
stream and parallel stream.

SPEAKER 0
Yep. Parallel stream, the the Java runtime will figure out the details for you. It will, it will make a determination based on, so you might actually find that it can't be done that way, maybe the the thing you're telling it to do just isn't very easily paralyzable. But in the case of I've got a collection of 1000 data elements, yeah, it'll figure out how to farm it out. Yeah, and the detail will be taken care of by the run time and you don't need to worry about it. Yeah Uh, and as it says here, we'll come back to that idea in chapter 7. OK, so I'm hoping at this point I've convinced you that writing things in a declarative way has a bunch of advantages. Yeah. Um, but it is a different way of structuring your code. Now the other thing you'll notice about this is that we've chained together multiple operations, we've said filter and then sort and then collect. Right, and these are all chained together as single operations, but they're glued together in the one expression, or the one line of code. OK. So we create what's called a stream pipeline. So we take the menu items, we filter them based on some criteria, a lambda, we sort them based on some criteria, another lambda. We might wanna map them, I haven't told you what a map is yet, we'll come back to that, and then accumulate the results. OK. So this gives you a lot of expressivity. Um You might also notice here, just looking at this, that looks very SQL like, right? Now the other thing that you, you will, if you, if I've not sent you to sleep already. Would you consider SQL low level or declarative? What do you guys think? Who would think that SQL is a low-level language? Or a low level way of describing the problem. Who thinks it's a high-level declarative way? Yeah, you're absolutely right. So you, you guys in some sense are already used to thinking about this, at least in terms of databases. OK, apologies to the vegetarians and the vegans in the room. The example goes into a lot of, uh, well, the example that runs through the rest of the book has a lot of meat products and unhealthy things, but that's OK. Most of us enjoy these, unfortunately. Unfortunately. OK, um. Now the other thing that you might wanna note, you. We'll come across bits of Java code that already exists where third parties have come along and said, hey, I'm gonna write an API that provides some kind of functional style data handling for Java. And that went on for a number of years and then the Java people said, hey, we're gonna, we're gonna include one in the standard library. But that didn't magically make the old code base disappear. So some of these same ideas exist in other libraries, uh, here's a few of them, um. But uh of course the implementation details and the APIs are slightly different, but the ideas are much the same. So it's declaritable, it's composable, we can chain things together, and we can get better performance through parallelization. OK. So for the remainder of the chapter, as I mentioned, we've got this list that we're gonna be working on as our example. And the dish itself Is um declared here. So the other thing to note about this, is that when you first come along and look at streams, oftentimes the examples are pitched as sequences of numbers, sequences of characters, so in other words, things that look a bit like a primitive type. But we're gonna actually have streams of any Java object of any level of complexity you'd like. OK. So I'll let you guys read through that in your own time. Thing to remember about the stream, it really does represent a sequence of elements, so it's a little bit like a collection. Often we think about collections this way. Things are in a particular order. They have a source, this is where we start diverging semantically from, Uh, what an array or an array list is, um, streams produce or consume consume data, so video feeds, stock market tickers, could be a feed of data coming from farm equipment, it could be anything, but the the the data is coming to you in real time. We have a bunch of operations that we perform on the stream. And they tend to have very much a similarity with the sorts of things we'll do with databases. And so things like filtering, which you've met already, you've seen sort, you can probably start guessing at what some of these are. And these two together are kind of interesting, I'll come back to that in just a moment, but these two in particular, were a really big deal back in the early 2010s, I guess it was, um. And it's kind of the start of this notion of big data and data science as a computing thing rather than a statistics thing. Now the other thing to note about. These streams as opposed to um. Collections, is the way you do iteration with them. So you guys met the iterator pattern, and you've been playing with iterators for a while now. What can you tell me about the iterator pattern? So you've got an array list and you want to iterate over an array list. How do you do that? Hm You iterate through the array, but how, what's going on? So let's let's remind ourselves of that iterator pattern. Alright, so the Wikipedia one, that'll do. So we look at the diagram. Yeah, that that's the one I want. What have we got? We have an aggregate, so that's our array list or our list or our set or whatever it is that we're dealing with. And we have an iterator that says, how's next, are there any more elements? And please give me the next element. So we have two separate classes that have a symbiotic relationship to each other. So when we actually iterate through an iterable collection, the aggregate over here. Oops, sorry. This thing over here, we're actually using another class, by proxy to actually do that iteration. So in other words, where we are in the collection, the state of where we currently are is kept somewhere else. Why, why do we do that? Why don't we have the iteration itself being handled by the collection? Why not put it all in the aggregate? What do you guys think? Nobody What if I want to have two different. Ways of accessing that data. I've got one loop iterating over a collection of data, and I've got a separate loop iterating over that collection of data. How would I implement that? the We have to It's a All right. I'm not talking about streams here, I'm just talking about iterables, collections. OK, so if I'm gonna have some collection of data, so let's just use an array list for something that we're all familiar with, and I wanna have something that lets me go and loop over all the elements of that array list, If I was to put that iteration into the array list, only one thing could keep track of where we are in the list. And I might want to have two independent bits of code looking at that same collection of data. So think of it from from a from a looping perspective, this is like the loop variable. The iterator is the loop variable, it's the 4 X in whatever. We don't want to tie those two things together. However, streams semantically different, streams are things that we're just basically pulling data off as it comes. So the thing that keeps track of the iteration is internal to the stream. So from a coding perspective, it already starts to look very different, even though it's doing something similar, it's a collection of data. It's not a collection in the Java sense that we've already talked about. It works very differently. OK, So let's have a look at another example here. We're gonna bring in, um, we're gonna go and create a list of the three high caloric dish names, so the three highest, most, Weight putting on meals in our collection. So we're gonna take our menu, which is an array list, we're gonna turn it into a stream, we're gonna filter it and we're gonna say we only want dishes that are more than 300 calories. But remember that the thing we're filtering. Are these things up here, dishes. So dishes have got an awful lot of data in them, a lot of different attributes, but what we want is a list of the names. So once we've actually filtered out the things. That are over 300 calories, the next thing we want to do is say, OK, let's just get the name, we just want that string. So we can map The dish to the get name method, and that says just execute the get name method from dish, on all the elements on the stream. But all the elements that have passed through the filter stage. But there might be more than 3 dishes that are greater than 300 calories, so we've got a criteria that we want. Things that are only 300 calories, but we're also saying we only want 3 dishes. So we're gonna limit that to 3. So this only gives us the 1st 3 elements of our list. Or our stream. We're then gonna turn it back into a list, and print it out. But notice, what we're getting back here is a list of, Well, what you tell me, what kind of list is that going to be? Is it gonna be a list of dishes? Is there gonna be a list of dishes?

SPEAKER 1
say Sorry? These theories say that we To The 3D that have bigger than 300.

SPEAKER 0
Sure, but what if I was to actually ask my Java debugger the result of this, what kind of list am I gonna get out? I'm gonna get a list of dishes? Listed the name of the dishes, and if we look at, The thing up here that says, get name, what does it return? String So even though we're going back into list land, after we've been doing some stream processing, we started off with a list of dishes, turned it into a stream. We did some processing on it, and the thing that we're gonna get back is a list of strings, and again, you'd hope so cos that's what we've been asking for up here. If that wasn't the case, the compiler would give you a type error. OK, so don't be fooled by this, it's not turn the list into a streams and then get the same kind of list back. We're saying here, turn the thing back into a list, but one of the stages in our pipeline is to go from dishes, To strings. Yeah OK. Alright, any questions so far? Yep. massive Every time it Element is added to the stream. Yeah, that's a really good question, so if these things are of indeterminate length, what does it truly mean to sort? Right, so that's a tricky question, and it depends on some circumstances. So if you actually look at the stream itself, well let's actually have a look. If we look at the. I'll use Java 17 because that seems to be the most. So we look at streams. And then we find. The sort. All the way down here. Now notice it says. Returns the stream consisting the elements of this stream are sorted, sorted according to natural order. So the question is what do we mean by the elements of the stream? So I think you need to think a little bit carefully about what that might mean. If I've consumed 10 number of elements. How many things can I sort? If there are other elements in the future that I have not read or received yet, could I sort them? Right, so the two possibilities are, if I said sort, one possibility would say, I have to wait for the stream to end. That could mean my program pauses forever. Or it could be, sort everything I know so far. Which is what actually happens. Yeah. But yeah, it's a really good question. OK. Alright, now this, mention this map and reduce. Notice that we've got this idea, this in this diagram, we've got something that looks a little bit like a, well, you can see here there's vertical things, we've got processes happening from the data coming in at the top. With performing an action, then performing another action, and then performing another action and finally doing a collection. So, implicitly the time arrow was going from top to bottom on the screen. Left to right, we've got more than one thing happening, so this is where our parallelism comes into play. In principle, we can do this on a whole bunch of, of things at once. But the last stage. Is to pull all the data together and put it in some kind of collection. So When I alluded to this MapReduce thing earlier. When I was a student, you could just, it was just becoming a thing that you could buy a computer that had more than one CPU. It cost a lot of money, things didn't work very well, and over time, over the last few years, last many years, people have managed to refine not just the hardware, but the software in how it deals with having multiple cores, to the point where, you know, how many cores have you got on your phone? I think mine's got 8, which is not too bad. Home computer can have a dozen, 20, even I think these days. So clearly the operating system and the software needs to know how to deal with this. But ultimately, when you do some sort of processing, the end result is probably just one set of data. And so when The entire large scale computing thing started really taking off in about 2008, 2009. Google were kind of the prime motivator. And they would take this idea of putting your data in the cloud, spreading your data over hundreds of computers, possibly thousands or more computers, doing some processing on it, and then giving you a result. And so this entire process is what they call map reduce. So so you spread your data and map it over multiple compute nodes. Do the calculations in lots of different places in parallel, and then give you a result, so reduce it down. So, not every single uh application works like this, but many, many large data workloads work on this model, map and reduce. And so the filter pipeline is built around that model. Alright, I think I've already talked to this about streams versus collection, so I don't think I'm gonna bore you silly with that. I think I've already made that illusion, there you go, DVD versus uh internet streaming. Um OK, now the other thing that's notable here is that you can only traverse a stream once. What do we mean by that? Well, it kind of alludes to what I mentioned earlier about iterators. I might have multiple iterators against the same collection, but any single iterator has the has next and next operations. What's the operation that intuitively is missing from that? If I could only have one iterator instead of multiple iterators against the collection, what other thing would I want to add to that list of methods? The, give me the next item, tell me if there's any more items left. What thing would I also want to do if I could only ever have one iterator? Previous Hm? Previous item. You're on the right track, the previous item, but you might wanna just have the rewind to the beginning method. Because if I wanna iterate more than once, if I go through the list, or the array or the set or whatever my collection is, and I get to the end, maybe I don't get to the end. And then later on in my code I want to reiterate over that, I need to start at the beginning again, don't I? But you notice that that's not what iterators have. Why don't iterators have it? Well, because I can create a brand new iterator, and it implicitly starts at the beginning. So iterators can only trans uh traverse a collection once. Streams have that similar behavior. You can only, Traverse a stream once. The reason for it is is that the data itself could be coming from a real-time process, and so there is no real notion of going back in time. Once the data is consumed, it's consumed, in other words. OK, um. So if you really want to think about this really deeply, they're basically saying yep, streams are a time-based thing, arrays are a space-based thing. Don't think too deeply about that, although I guess I could give you that as a dis a distinction question in the final exam. Actually I might, see how we go. Uh, I already referred to this notion that, um, iteration happens internally in the, the stream rather than externally in a collection. What else to mention here? Yep, there it is, the diagram diagram for it. So in other words, there's no for each possibility with a, with a stream. You're just consuming the data as it comes. Um, alright. Are we going for time now? Now the other thing to note more fully about this is that those lambdas can be any old kind of lamb that we like as long as they produce the desired type. So for filtering, we're really interested in is the data included or excluded, so it's a boolean, and indeed if you remember the functional uh API for Java. You'll find that filter indeed is a predicate, in other words, a method that produces or returns a bullion, I should say. But the others. Don't have necessarily predicates, so we have lots of predicates, but we have things that will also just take general functions that return integers, um, there are things that will consume the data, in other words they return a void, so these are all have to be matched correctly when you're writing your code. All right. Oh yes, sorry, a little bit of jargon I forgot to mention earlier. So when we look at one of these pipelines, we have what we call intermediate operations. So the intermediate operations are the things where we've avoided creating, A garbage or a junk variable. We haven't had to create a temporary variable name, because we're literally passing the data by chaining the method calls one after the other. But ultimately, we're gonna collect that data back into some kind of collection, and we call that the terminal operation, so the intermediate operations in a pipeline, and then the final operation is what's referred to as the terminal operation. So intermediate operations generally form that same notion, filtering, mapping, limiting, sorting, and so forth, but the last terminal operation is almost always a collect. Doesn't have to be, but most of the time it will be. OK, the other one that you might see from time to time is this one. So you can actually do it for each on the stream, and what that's doing is it's doing the same idea as a collect, but rather than just saying hey lump it back into a an array list or a set, take it and turn it back into, Some kind of collection, and let me iterate over the items. Alrighty. I think that's it for chapter 5, yep. So any questions before I move on to chapter, sorry, chapter 4, chapter 5. No, Happy? OK, can I take a straw poll at this point, cause as I said, this is quite a mental shift. How are you, how many of you are going, uh, I feel like I get it, I'm not asking, do you think you could sit down and write it right now, but how many of you are like, OK, that makes some sort of sense. Is anybody kind of just like, what the? Nobody's at that point yet. OK, one of them's, I'm glad you're honest. Come talk to me at the end, we'll try and straighten that out. OK. Um, now I'm not gonna spend a great deal of time over chapter 5, I just want to point out some of the other things that you might want to do with a stream. So, you want to filter them, we already saw that, but you might wanna do what's called sli slicing and mapping, and then matching and reducing, so we'll have a look at that. We'll also look at, The same problem that cropped up with collections crops up with streams. Streams are great, everything is a stream of objects. But what do we know about Java? There are objects and there are. Primitive types, so can we do something with primitive types and streams, it turns out yes you can, but of course there's special methods to deal with that. Um, creating streams from multiple different sources and then I said, as I said, in some print, in some ways streams are infinite, but we'll talk about the notion of what actually happens if you're dealing with a truly infinite stream of data. OK, so filtering, I think you've kinda gotten the general idea of, you pass it a predicate, a lander that just says take each element of the stream and then make some sort of test that returns true or false. That's relatively simple, I think. Um, now, the other thing that you might wanna do is not just filter, but you might want to filter so that things are distinct. And so just like you can in SQL, you can actually pass in an operation that says dot distinct. So there's a method there and it works in just the same way as you would expect. OK, now this is something they added a little later on, so you don't get this in Java 8, but in Java 9, so it's in the Java that you guys are using. But you can actually do something a little bit weird here, you can slice. So what does that really mean? Well, it means I can take not just an individual, uh sorry, let me start over again, not looking at each element of the stream. By themselves, but I can say, keep giving me the values of the stream until some pre uh predicate is met, or only start capturing or doing something with the stream once a predicate has been met. So you can do some sort of bracketing on the stream, and they call that slicing. And so it's in the names here, take a while. So again, let's imagine that we've got this list of 5 dishes in our stream. We're going to Do that same um filtering, things that are less than 320, we're gonna turn that into a list. But We already had it sorted on the number of calories. Um, and so filtering requires you to, to go through the entire list, which is a little bit cumbersome. So what we can do instead is actually use the take while method. And what it does is it only grabs the values once that predicate has been satisfied. So think of it like a gate. It'll block everything until. That predicate is met. Why, what do you think is the value of this rather than just using the filter? What benefit do you think that brings? Anybody? size now? It's not so much keeping the erasos down, but this kind of ties in with your question about sourcing earlier. Obviously in the background the API has to be doing a lot of clever stuff, right? So you've got um, We've said that you can avoid having these garbage or temporary variables. But somewhere in the background on the API, the guys who invented this, Had to actually create some temporary variables somewhere, right, they had to store stuff momentarily, and then they probably had to iterate over it. But if my data is coming pre-sorted, because I've already applied a sort method to it, I don't need to test every element. I don't need to iterate over it multiple times. Once, if I know my data's sorted, once it hits a particular point, I don't have to test that predicate again, and that's what these do. So even though declaratively it looks very similar, it's actually saving me a lot of compute resources. So, let me unwind a little. At the beginning I was saying how great this declarative way of writing code is. It takes the burden off you, the programmer, I don't have to remember how many times I have to go through a list, I don't have to specify this very low level way of doing things. But the downside to this declarative way of programming is it can be really, really inefficient from a space and or a time perspective. So if I don't have my data pre-sorted, and I have a predicate, underneath the hood, Java's probably going over and over and over that stream of data multiple times, it has to buffer it, it has to iterate over it multiple times, and it's just gonna be slow, and it's gonna consume lots of memory. So by making sure that I've got this kind of predicate available to me. It saves that internal storage buffering and recalculation of data. So it gives me better time and space efficiency. So you don't quite get a free lunch out of it. This is the converse, it's dropped while, so it's going to not allow the uh data stream items through until the criteria is met. The limit truncates and it just says after this many, I don't care about the rest of the stream. So you've already met that. It's probably not worth talking too much more about the rest. Um, OK, skip lets you skip incoming entries. We got here filtering. OK, this is what I was looking for, so map, I've kind of glossed over a map and it's time to unpack that. So what's Matt do? So we saw in our original version, the the Java 7 version, we had multiple loops, one on top of the other. So iterating over the data, we had to create multiple temporal, uh, temporary variables. And one of the reasons we wanted to do that is that, well, we started off with a list of dishes and we wanted to get a list of strings out of it. So in order to do that, we needed to do the dish get name method. Now if we do that in a regular array. We'd normally do that, let's process our our incoming array elements as dishes, and then once we've got the temporary array of the dishes we want, we then have another loop that says, hey, take that list of dishes and give me back a list of strings. Right, so it's gonna have that double iteration. Here, we want to do it in line. So what we need to do is insert a method that says, hey, give me, This conversion between one data type and the other. And that's what MAP does, it lets us actually run an arbitrary function over the stream. So for each element in the stream we're going to run. This method. And the thing that we pass in is a lambda, but of course if you've got one of the pre-built methods that will do the job for you, you can use this syntax. To pull it out of the class. But you can put any lambda in there you'd like, as long as it's gonna match the um, The type constraints. Sorry? It doesn't compare, it says literally for every element of the stream. Run this method on it. Yeah. Calling the method name. That's why the class is there, yep. OK, and you can put any anything that you'd like, again, as long as it's gonna pass the compiler's type checks, so I can do string length, I can map that over, um, anything I'd really like. Now, another thing that you might wanna do. Is rather than run a single, Pre-canned method, I might wanna run some kind of lambda over it, and so one of the things you might want to do is what we call flattening the stream. So, if I was to say I had a stream of characters, so let's go back to the notion of a primitive type, I've got a stream of characters coming in. Each element in the stream is a separate data item. But what do we know about a sequence of characters, what do we normally call that? The string Right, and so we've got this notion of data item, data item, data item, data item, data item, and we wanna concatenate those and produce myself a single data item which is the string. Now, doing that with single characters producing a string is a relatively intuitive idea, but you might want to do something similar with much more complicated data, but the same principle applies, and we refer to that as flattening. So rather than returning this list, We'd like, sorry, this, this uh this word, we want to actually return the elemental uh strength. So we can do that by using this kind of approach. So we're gonna use uh a method that's built into uh the string, which is called the split method, and it lets us split it on here in this, this case, it's an empty string. So passing an empty string just says split it in every character. So we're gonna get a full array of those. Uh, letters. Alright. See if there's anything else I wanna go over in this chapter. Think I'm pretty close to what I wanted to cover here. Um Yeah, OK. The other thing I wanted to cover before I let you guys go, cos I only want one more thing to do I think. Let me just check that there's nothing else I really wanted to do. I mean you guys can go over this yourselves, looking at maximum or minimum, I think these are relatively straightforward after what you've already seen, but the main thing I wanna come back to. Yep, chapter 6 is collecting data. And so I've already referred to this, but I wanna just talk about this in a little bit more depth. So this notion of collect, the the last step, reducing and giving us back a collection of some kind. So If we wanted to do something. A little bit like an SQL courier that we saw earlier. We might wanna do something like this, we're gonna take some kind of transaction, so we're thinking of financial transaction here, based on currencies, and we're gonna generate a a hash map out of it. You guys know what a hash map is? Anybody not familiar with a hash map, we've seen it a couple of times in the unit, but I don't think you've spent a lot of time fleshing it out. So it's another form of collection, but what it allows you to do is instead of saying with an array, I'm gonna get element 0, element 1, element 2, I'm gonna be able to say, I wanna pick out an element based on some other value. So I can look things up by a keyword, essentially. Sometimes they're known as associative arrays. So we're basically gonna generate a hash map which says map a value in a currency. To a list of transactions. And then we're gonna go through and build this. Um, This list, or sorry, this this hash map, and we're gonna do some comparisons. So again, we're going back to the older style of doing things one step at a time with multiple loops. So let's actually have a look at how that might be turned into something of that streaming style. We're just gonna pass in the transactions, turn them into a streams, and then collect and group by a particular transaction. So we're pulling all that complexity out and putting it into something that is really quite readable here, even though the lines are a bit wide. So let's have a look back at this again. We're building our, we're creating an empty hash map. We're gonna go through and grab for every transaction, figure out what the currency that the transaction has occurred in. Create a list of transactions for that currency, so here is our wasted temporary variable. If it's equal to nil or null. We move on to another array list. And we build this hash map that way, no, I don't want that, go away. OK, so we're doing that very iterative thing again, but, The whole notion is build. Construct the uh the data items into our uh collection. And then add them iteratively. Here we're doing the same thing, but in a much more expressive way. And you'll notice that we get all the usual things that we'd normally want to do when we create a collection, and be able to group by or sort by or the other kind of aggregation functions that you would have in a database. All right. OK, so in terms of these collectors, long story short, the collectors themselves are things that generally uh relate to the existing collections in Java. So there are sets, there are hash maps, there are array lists and so forth. So I can generate each of those at the tail end as the, the terminating step of my pipeline. So I've got 2 lists, 2 sets, and so forth.
